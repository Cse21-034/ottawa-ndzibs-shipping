services:
  - type: web
    name: ottawa-ndzibs-backend
    runtime: node
    plan: starter
    buildCommand: npm install && node build-backend.js
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
    healthCheckPath: /api/services